import LeanConfinement.Operator

namespace LeanConfinement

-- CP1 keeps expectation invariance explicit in `ExpectationInvariant`.
-- No action/measure or path-integral derivation is encoded at this checkpoint.

end LeanConfinement
